/**
 * SiliconPath — Type definitions
 *
 * This platform is a public VLSI learning site with no authenticated features.
 * All content types live in:
 *   src/lib/academy/  — LearningTrack, LearningDay, LearningQuestion, etc.
 *
 * This file is intentionally minimal. Add types here only when they are
 * shared across multiple non-academy features.
 */

export {}; // ensure this is treated as a module
